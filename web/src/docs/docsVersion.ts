/**
 * Documentation version constant
 *
 * This is the single source of truth for the current documentation version.
 * When the documentation is updated to reflect a new framework version,
 * update this constant accordingly.
 *
 * Future: This will evolve into a version system when multi-version docs are introduced.
 */
export const DOCS_VERSION = 'v0.1.0-alpha';
