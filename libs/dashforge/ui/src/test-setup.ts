import '@testing-library/jest-dom/vitest';
import { setTheme } from '@dashforge/theme-core';
import { defaultLightTheme } from '@dashforge/tokens';

// Initialize theme for tests
setTheme(defaultLightTheme);
